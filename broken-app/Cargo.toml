[package]
name = "broken-app"
version = "0.1.0"
edition = "2024"

[dependencies]
pprof = { version = "0.13", features = ["flamegraph"] }

[dev-dependencies]
criterion = "0.5"
pprof = { version = "0.13", features = ["criterion", "flamegraph"] }

[[bench]]
name = "baseline"
harness = false

[[bench]]
name = "criterion"
harness = false

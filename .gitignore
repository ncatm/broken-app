# Rust / Cargo (workspace target в корне)
/target/
**/*.rs.bk
*.pdb

# macOS
.DS_Store
**/.DS_Store

# Редакторы и IDE
.idea/
.vscode/
*.swp
*~

# Локальные снимки с вложенным репозиторием (не коммитить)
broken-app-before/.git/

# Артефакты проекта (логи, бенчмарки, OPTIMIZATIONS.md) — в репозитории, не игнорировать.
